# Test-only requirements. The integration itself needs only what manifest.json declares
# (aiohttp); these tests import the integration against the Home Assistant it will run under,
# which is the point — the bug they regress was an import-time failure.
pytest>=8.0
homeassistant>=2024.1.0
